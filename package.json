{
  "name": "medikiosk",
  "version": "0.1.0",
  "private": true,
  "description": "AI-Powered Clinical History & Patient Intake Platform",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:turbo": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@11.23.0",
  "dependencies": {
    "@tailwindcss/postcss": "^4.3.3",
    "tailwindcss": "^3.4.19"
  }
}
